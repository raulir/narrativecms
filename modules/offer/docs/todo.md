# Offer module backlog

**Legend:** `[ ]` open · `[x]` done

- [ ] **Needs shop module** — offers should be able to use shop (discounts, product links, catalogue). Not wired yet; `offer/bar` is heading + link + emotes only. When this lands, keep shop as a real module dependency (enable shop before offer).
- [ ] **Timmy look via extend** — `offer/bar` SCSS currently bakes site chrome (`sanders` font, `#fff6ea` sidebands). Split to generic offer CSS + Timmy `extends` of `offer/bar`.
