# Menu module backlog

Generic frontend site menu (`menu/menu`) and companion menucross assets.

Split from `basic` so brochure sites can enable nav without the rest of basic.

**Legend:** `[ ]` open · `[x]` done

---

- [x] Split from `basic` → standalone `menu` module (`menu/menu`)
