{
	"label": "Product item",
	"list": {
		"list_title": "Product items",
		"item_title": "Product item",
		"title_field": "heading",
		"filter_fields": {
			"show": "-- show --"
		},
		"link_target": "0"
	},
	"item": [
		{
			"type": "fk",
			"name": "product_id",
			"label": "Product",
			"list": "shop/product"
		},
		{
			"type": "text",
			"name": "price",
			"label": "Price",
			"help": "Leave empty for product default price"
		},
		{
			"type": "text",
			"name": "sku",
			"label": "SKU"
		},
		{
			"type": "text",
			"name": "number",
			"label": "Number",
			"default": "1",
			"help": "Listed stock. Available = this minus qty on unfinished order lines that point here. Can go negative until those orders are Finished."
		},
		{
			"type": "repeater",
			"name": "dims",
			"label": "Dims",
			"help": "One row per dim. Value is the value id from the dim list (custom picker later).",
			"fields": [
				{
					"type": "fk",
					"name": "product_dim_id",
					"label": "Dim",
					"list": "shop/product_dim",
					"add_empty": "1"
				},
				{
					"type": "text",
					"name": "value",
					"label": "Value",
					"help": "Value id from the dim, e.g. {a4} or {black}"
				}
			]
		}
	]
}
