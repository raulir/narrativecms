# Agent entry (project root)

This file is a **stub only**. It must **not** be filled with documentation.

All documentation belongs in **module `docs/` folders** (Markdown).

## Where to read

| What | Path |
|------|------|
| **CMS / system conventions** (panels, models, style, images, video, …) | [`modules/cms/docs/agents.md`](modules/cms/docs/agents.md) |
| **Other modules** | check: modules/<module>/docs/agents.md |

CMS runtime detail: [`modules/cms/docs/system.md`](modules/cms/docs/system.md). Other CMS topics: [`modules/cms/docs/`](modules/cms/docs/). Module backlogs: [`todo.md`](todo.md) (index of `modules/*/docs/todo.md`).

## Rule for agents and humans

- **Do not** update this root `AGENTS.md` with new documentation, conventions, or how-tos.
- Put CMS/framework knowledge in `modules/cms/docs/*.md` (or other modules’ `docs/` when module-specific).
- Put **project-specific** agent notes in the main site module’s docs (here: `modules/music/docs/agents.md`).
- When working in a subdirectory, also check for additional `AGENTS.md` / docs deeper in that tree.

## Project backlog

- Any backlog belongs to modules/<module>/docs/todo.md
